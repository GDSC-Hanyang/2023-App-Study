GestureDetector -> 모든 제스처를 감지한다 .. OnTap 사용자가 탭을 하면 발생한다 up down 으로 스크롤 가능

Navigator -> detailscreenwidget을 화면 위로 올려줌

Hero -> container 를 감싸면 애니메이션등을 통해 단조로운 디자인을 좋게 바꾸기 가능

url_launcher -> flutter에서  http 를 열게 해줌

flutter pub -> 패키지 매니저로 pub을 통해 url_launcher, shared preference 등 필요한 패키지를 불러 올 수 있음.



// 소감 :  class로 대부분의 것을 만들 수 있다니 확실히 사용자 친화적인 flutter의 매력이 느껴졌다.
